import RequestProperties from './requestproperties';
import Response from './response';

const MainPane = () => (
    <div>
        <RequestProperties />
        <Response />
    </div>
);

export default MainPane;